#include <stdio.h>
#include <cs50.h>


int main(void){

    int arr[] = {3,4,2,1,6,5};

    int count = 6;

    int holder;

    for(int i = 0; i<(count/2);i++){


    }

}

int length_of(int *arr){
    return sizeof(arr) / sizeof(arr[0]);
}

void merge(int *arr){
    if(length_of())
    int mid = length_of(arr)/2;//3
    int first_arr[3];
    int second_arr2[3];
    for(int i = 0; i < length_of(arr); i++){
        if(i<mid){
            first_arr[i] = arr[i];
        } else {
            second_arr[i] = arr[i];
        }
    }

    return
}